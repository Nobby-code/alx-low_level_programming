#include "main.h"
#include <stdio.h>

/**
 *main -  the program entry point
 *@argc: argument(s) count
 *@argv: argument(s) vector
 *
 * Return: 0 on success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
